meta {
  name: Batch Upload Files
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/collections/{{collectionId}}/files
  body: multipartForm
  auth: bearer
}

headers {
  Content-Type:
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  files: @file()
}
