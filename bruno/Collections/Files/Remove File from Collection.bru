meta {
  name: Remove File from Collection
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/collections/{{collectionId}}/files/{{fileId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
