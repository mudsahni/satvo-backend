meta {
  name: Get Collection
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/collections/{{collectionId}}?offset=0&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
