meta {
  name: Create Collection
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/collections
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Q4 Reports",
    "description": "Quarterly financial reports"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("collectionId", res.body.data.id);
  }
}
