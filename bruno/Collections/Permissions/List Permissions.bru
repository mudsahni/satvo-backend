meta {
  name: List Permissions
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/collections/{{collectionId}}/permissions?offset=0&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
