meta {
  name: Set Permission
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/collections/{{collectionId}}/permissions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "user_id": "{{userId}}",
    "permission": "editor"
  }
}
