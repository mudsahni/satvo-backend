meta {
  name: Remove Permission
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/collections/{{collectionId}}/permissions/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
