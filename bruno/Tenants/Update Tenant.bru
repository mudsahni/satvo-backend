meta {
  name: Update Tenant
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/admin/tenants/{{tenantId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Acme Industries",
    "is_active": true
  }
}
