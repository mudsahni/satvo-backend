meta {
  name: Add Tags
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/documents/{{documentId}}/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "tags": {
      "vendor": "Acme Corp",
      "category": "utilities"
    }
  }
}
