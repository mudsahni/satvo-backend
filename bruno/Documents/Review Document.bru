meta {
  name: Review Document
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/api/v1/documents/{{documentId}}/review
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "status": "approved",
    "notes": "Data verified against source document"
  }
}
