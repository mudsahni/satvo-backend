meta {
  name: List Documents by Collection
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/documents?collection_id={{collectionId}}&offset=0&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
